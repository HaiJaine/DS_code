#include <iostream>

using namespace std;

//定义双链表结点类型
typedef struct DNode {
    int data;//数据域
    struct DNode *prior, *next;//前驱和后继指针
} DNode, *DLinkList;

int main() {
    cout << "";
    return 0;
}